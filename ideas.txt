objeto que maneje el grafo

spreadsheet.add(A4, entry);

crear vertices para cada celda desde el inicio
enlazar si hay una referencia a otra celda
si se modifica una celda esxistente, se busca con cuales esta enlazado para actualizar valores



ideas:
- asociar valores a enum de column para poder usar numeros. Podria evitar un objeto celda
- usar hashmap en vez de arraylist para los vertices del grafo. ASi se puede usar luegp los ENUM para extraer un vertice en O(1)
